git branch gh-pages

git checkout gh-pages

git push origin gh-pages

in gh-pages branch; add 

touch .nojekyll

git add .nojekyll

git commit -m "created .nojekyll file"

git push origin gh-pages



to view presentation 

atidmore.github.io/repo/filename.html